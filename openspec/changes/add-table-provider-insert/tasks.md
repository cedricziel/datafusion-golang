## 1. FFI surface

- [x] 1.1 Extend `include/datafusion_go.h`: `df_session_register_table` gains `uint8_t supports_insert` after `supports_pushdown`; declare `go_table_insert(uintptr_t handle, struct ArrowArrayStream *in_stream, int32_t insert_op, uint64_t *rows_out, char **error_out)` with documented ownership (stream ownership transfers to Go on entry; `rows_out` written only on success; error string malloc'd, freed by Rust) and the `0/1/2 = Append/Overwrite/Replace` mode encoding (design D2, D3, D5)
- [x] 1.2 Update the Rust FFI declarations in `rust/datafusion-c-abi/src/ffi.rs` (new `go_table_insert` extern, extended `df_session_register_table`) and extend the test-stub trampolines: an insert-recording stub that consumes the incoming stream and records batches, insert_op, and reported count; variants that error immediately, error after consuming some batches, and reject non-`Append` modes (design D2, D5)
- [x] 1.3 Update the Go trampoline side in `datafusion/table.go`: `//export go_table_insert` importing the incoming stream as an `array.RecordReader` via `cdata` (reverse direction of `go_table_scan`'s export), mapping `insert_op` to the Go `InsertOp` constants (loud error on an unknown value), calling `InsertInto`, writing `rows_out`, and releasing the reader after the call returns regardless of outcome; `defer trapCallbackPanic` per the existing convention (design D2, D5; spec: panic scenario)

## 2. Rust adapter: insert planning and execution

- [x] 2.1 Write failing Rust tests in `rust/datafusion-c-abi/src/table.rs` against the stubs: `INSERT INTO t VALUES ...` on an insert-flagged handle reaches `go_table_insert` with `Append` and the full registered schema, and the statement result is one row `count: UInt64` matching the stub's reported number; the same statement on a non-flagged handle fails with the "does not support INSERT" error without invoking the stub, and the session stays usable (design D1, D3; spec: opt-in scenarios, count scenario)
- [x] 2.2 Implement `GoTableProvider::insert_into`: return the `not_impl` "does not support INSERT" error when `supports_insert` is unset; otherwise validate the input schema (`logically_equivalent_names_and_types`) and return `DataSinkExec::new(input, Arc::new(GoDataSink), None)` (design D1, D3)
- [x] 2.3 Implement `GoDataSink::write_all` with the bounded-channel bridge: pump task forwarding input batches/errors into the channel, a synchronous `RecordBatchReader` over the receiver exported as an `FFI_ArrowArrayStream`, the `go_table_insert` call under `spawn_go_blocking`, and — unconditionally, on success and error alike — awaiting the pump task before returning (design D4, D6)
- [x] 2.4 Add Rust tests for the remaining engine paths: `INSERT INTO t SELECT ...` sourcing from another registered stub table delivers that table's rows; a mid-stream input error (failing source scan) surfaces as the statement error with the pump joined and the session usable; a stub insert error (immediate and after partial consumption) fails the statement and leaves the session usable; `INSERT OVERWRITE` delivers `Overwrite`; `Replace` is exercised at the trampoline level if the SQL dialect refuses `REPLACE INTO` (design D4, D5, D6; spec: input-stream, mode, and error scenarios)

## 3. Go public API and end-to-end tests

- [x] 3.1 Write failing Go tests in `datafusion/table_test.go` with an in-repo writable memory provider (records received `InsertOp` and batches; makes committed rows visible to its own scans; commits only after draining the reader): `INSERT INTO t VALUES (...)` returns a `count` result and a following `SELECT` on the same session sees the rows; `INSERT INTO t (name) VALUES ('x')` delivers a full-schema row with NULL `id`; `INSERT INTO dst SELECT ... FROM src` across two registered providers; zero-row insert reports `count = 0` (spec: opt-in, input-stream, and count scenarios)
- [x] 3.2 Implement `WritableTableProvider`, the `InsertOp` type and constants, and registration-time capability detection (type assertion feeding `supports_insert`) in `datafusion/table.go`, composing independently with `PushdownTableProvider` (design D3, D5)
- [x] 3.3 Add Go tests for the failure modes: insert against a non-writable provider errors without invoking it and the session stays usable; a provider rejecting `InsertOverwrite` fails an `INSERT OVERWRITE` statement with its message; provider error after partial consumption and provider panic both surface as statement errors with the session usable; insert is never invoked after `Close()`; concurrent inserts and scans against one provider pass under `go test -race` (spec: mode-rejection and error scenarios; design D6)
- [x] 3.4 Verify read-path behavior is untouched end to end: existing `datafusion`, `providers/parquet`, `providers/iceberg`, and example tests pass unmodified, and `INSERT` against the (still read-only) parquet/iceberg providers fails with the clear "does not support INSERT" error

## 4. Docs and wrap-up

- [x] 4.1 Document the insert contract on the Go types: at-most-once invocation per statement, reader ownership and mandatory release, commit-or-rollback before returning, partial-write responsibility, goroutine-safety extended to scan-during-insert with provider-defined visibility (design D2, D4 risks; spec: error requirement)
- [x] 4.2 Update README: writable-provider opt-in interface, supported statement forms and modes, the `count` result convention, and that writable `providers/parquet`/`providers/iceberg` is planned follow-up work
- [x] 4.3 Run `make lint && make format`, ensure `go test ./... -race` and the Rust test suite pass, clean tree
- [x] 4.4 Commit series (semantic commits: FFI surface, Rust adapter, Go API + tests, docs)
